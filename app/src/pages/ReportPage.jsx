import { Typography } from '@mui/material';

export default function ReportPage() {
  return <Typography variant="h5">Báo cáo buổi — Trang đang phát triển. Upload file, tóm tắt AI, điểm/nhận xét, copy ảnh, sắp xếp theo điểm danh.</Typography>;
}
