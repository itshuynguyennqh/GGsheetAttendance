import { Typography } from '@mui/material';

export default function AzotaPage() {
  return <Typography variant="h5">Quản lý Azota — Trang đang phát triển. Cấu hình token, map lớp, pull/push học sinh, báo cáo trạng thái sync.</Typography>;
}
